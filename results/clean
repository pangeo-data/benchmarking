#!/bin/sh
for idx in cheyenne hal24 ;do
find $idx -type d |xargs -I dir mkdir 'clean-'dir
for i in `ls -1 $idx/*/*.csv `;do
 newfile='clean-'${i}
 echo "=== ${i},$newfile"
sort -r ${i} |uniq -c |sort -r > ${newfile} 
#exit
done
done
